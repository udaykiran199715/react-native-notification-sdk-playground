# Notification Automation Backend

This folder contains the Firebase Cloud Functions backend used by the React Native Notification SDK.

It is automatically copied into the consumer project by the SDK CLI.

Do not modify this folder directly inside your application.
